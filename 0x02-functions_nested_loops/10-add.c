#include "main.h"
/**
 * add -  adds two integers and returns the result
 * @a: integer added to b
 * @b: integer added to a
 * Return: answer(a + b)
*/
int add(int a, int b)
{
int c;

c = a + b;

return (c);
}
